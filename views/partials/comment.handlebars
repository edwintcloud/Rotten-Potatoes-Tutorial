<div class="card" id= "{{this._id}}">
    <div class="card-block">
        <h4 class="card-title">{{this.title}}</h4>
        <p class="card-text">{{this.content}}</p>
        <!-- Delete link -->
        <p>
            <form method="POST" action="#">
                <button class="btn btn-link" type="button" id="deleteComment" onclick="commentDelete();" comment-id={{this._id}} movie-id={{this.movieId}}>Delete</button>
            </form>
        </p>
    </div>
</div>
