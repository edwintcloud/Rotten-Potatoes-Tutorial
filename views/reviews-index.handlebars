<h1>Reviews</h1>

<div class="row">
  {{#each reviews}}
    <div class="col-sm-3">
      <h2><a href="/movies/{{this.movieId}}/reviews/{{this._id}}">{{this.title}}</a></h2>
    </div>
  {{/each}}
</div>
